<title>My Fuel Logs</title>
<table class="spacer">
  <thead>
    <% @fuel_logs.each do |fuel_log| %>
      <tr>
        <% if fuel_log.user_id = current_user.id %>
          <th>Fuel Type</th>
          <th>Gas Price (Price per Litre)</th>
          <th>Price Paid</th>
          <th>Mileage at Fill Up (km)</th>
          <th>Car Make</th>
          <th colspan="3"></th>
        <% end %>
      </tr>
    <% end %>
  </thead>

  <tbody>
    <% @fuel_logs.each do |fuel_log| %>
      <tr>
        <% if fuel_log.user_id = current_user.id %>
            <td><%= fuel_log.fueltype %></td>
            <td><%= fuel_log.gasprice %></td>
            <td><%= fuel_log.price %></td>
            <td><%= fuel_log.mileage %></td>
            <td><%= Car.where(id: fuel_log.car_id).pluck(:make).join %></td>
        <% end %>    
      </tr>
    <% end %>
  </tbody>
</table>

  <h2> Please visit My Cars to make a new fuel log or 
  <a class="btn btn-lg btn-primary" href="/new_car" target="_self">Add a New Car!</a>
  </h2>
